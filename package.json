{
  "name": "harpex-geo-real3d",
  "version": "0.3.2",
  "private": true,
  "description": "Harpex Geo v0.3.2 — centro operacional urbano 3D com edificações reais OSM/OpenMapTiles e fallback local.",
  "type": "module",
  "scripts": {
    "preview": "node scripts/serve.mjs",
    "validate": "node scripts/validate.mjs",
    "hash": "node scripts/hash.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
